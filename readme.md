# Generate Docx Program
Highlight each name in every new document

## Requirement
```
sudo apt-get update
sudo apt-get install libreoffice

pip install python-docx
```

## Running

Create `/docx` and `/pdf` folder

```
python3 generate.py
```